# ABOP — Экранная раскладка для прототипа

**Назначение:** пошаговая раскладка каждого экрана среды ABOP, чтобы дизайнер отрисовал **точные мокапы и кликабельный прототип**. Для каждого экрана: зоны layout, компоненты (из UI-kit), состояния (пустой/загрузка/ошибка), и **кликабельность** (что нажимается → куда ведёт).
**Связка:** токены и компоненты — `ABOP_DESIGN_KIT.md`; продукт/вкладки/FR — `ABOP_PRD.md` v1.1; бренд/маскот/спиннеры — `docs/brand/`.
**Референс структуры:** LUDA MIRA v3 (сетка, вкладки-маршрут, паттерны экранов — переиспользуем, перекрашиваем в бренд ABOP).
**Статус:** v1.1. Приоритет отрисовки: **MVP = §3 Пульт → §4 Навыки → §5 Данные**, затем §2/§6/§7/§8, затем **§8а Эксплуатация/Флот** (надстраивается над рабочим одиночным прогоном).

Легенда зон: `▓` акцент/активное · `░` вторичное/стекло · `·` мета/mono · `[ ]` интерактив.

---

## 0. Общий каркас (Shell) — КАНВА-ЦЕНТРИЧНЫЙ (ADR-030)

**Корневая модель:** интерфейс — не 8 вкладок-ровней, а **один граф (канва) в центре** + палитра слева (кормит его) + инспектор справа (настройки узла) + **линзы** (способы смотреть на тот же граф) + **конверт-рамка** вокруг (всегда видима). Всё нативно вытекает из центра. Обоснование и оговорки — ADR-030.

```
┌─ КОНВЕРТ-РАМКА: 🛡 автономия A2 ≤ контракт · egress: sLAVA · HITL: 2 точки ·········· 🟢 в норме ─┐
│┌──────────────┬────────────────────────────────────────────────┬────────────────────────────────┐│
││ ПАЛИТРА      │ TOPBAR: крошки графа · ⌘K ······· [тема][расход]│  ИНСПЕКТОР (узел/связь, 320px) ││
││ (источники)  ├────────────────────────────────────────────────┤                                ││
││ ▓ лого ABOP  │  ЛИНЗЫ: [Строю][Запускаю][Эксплуатирую][Конверт]│  выбран: узел «Скоринг»        ││
││              │                                                  │  агент·навыки·автономия·cost   ││
││ 🔍 фильтр    │         КАНВА — ГРАФ (drag-and-drop центр)       │  ── настройки inline ──        ││
││ АГЕНТЫ ▸     │                                                  │  [mode][egress][cite]          ││
││  роль-чип →  │    [Источник]──contract──[Агент]──▶[Действие]    │  слайдер прав read→write→act   ││
││ НАВЫКИ ▸     │        ░узел      ░связь      ▓активный           │  (макс ≤ конверт, залочен)     ││
││  навык-чип → │         └ «Волны» на работающем узле (Запускаю)  │                                ││
││ ДАННЫЕ ▸     │                                                  │  на связи: data-контракт /     ││
││  рецепт →    │                                                  │  handoff (клик по ребру)       ││
││ КОННЕКТОРЫ ▸ │                                                  │                                ││
││ ───────────  │                                                  │                                ││
││ 🐵 Эйп «дум.»│                                                  │                                ││
││ @user·роль   │                                                  │                                ││
│└──────────────┴────────────────────────────────────────────────┴────────────────────────────────┘│
└────────────────────────────────────────────────────────────────────────────────────────────────────┘
```

**Три постоянные зоны + рамка:**
- **Конверт-рамка** (сверху/вокруг, всегда видима — оговорка 1 ADR-030): статус конверта графа — автономия ≤ контракт, egress-политика, число точек HITL, семафор здоровья. **Не прячется в узлы** — это value prop; клик разворачивает детали (→ §7 Паспорт как оверлей).
- **Палитра** (слева, 240px, стекло): источники для drag-and-drop В граф — **Агенты·Навыки·Данные·Коннекторы** (раскрываемые группы, чипы-источники); `🔍`-фильтр; внизу **маскот Эйп** (статус активного прогона) + @user·роль. Палитра заменяет бывшие вкладки Навыки/Данные/Коннекторы — они теперь источники, а не пункты назначения.
- **Канва** (центр): граф — узлы (источник/агент/действие/LLM) + связи (data-контракт/handoff). Drag-and-drop — основной жест. Активные узлы — индиго-подсветка + «язык ожидания» (Волны) в линзе «Запускаю».
- **Инспектор** (справа, 320px, по клику): настройки выбранного **узла** (агент/навыки/автономия-слайдер/mode·egress·cite/cost) или **связи** (data-контракт/handoff). «Настройки внутри элементов или между ними» — буквально.

**Линзы (topbar) — один граф, разные способы смотреть (ADR-030):**
| Линза | Что показывает на том же графе | Заменяет б. вкладку |
|---|---|---|
| **Строю** | авторинг: палитра активна, drag-and-drop, LLM-черновик потока | Канва |
| **Запускаю** | рантайм-оверлей: волны подсвечивают узлы, доска/аудитор/HITL на узлах | Прогоны/Пульт |
| **Эксплуатирую** | флот: semantic-zoom того же графа до Организация→процесс, живые Instance | Эксплуатация/Operations |
| **Конверт** | governance-оверлей: рамка+потолок автономии на каждом узле, аудит-след | Паспорт (как оверлей) |

- **Topbar**: крошки графа (Организация ▸ процесс ▸ агент/узел — общие с зумом линзы «Эксплуатирую»); `⌘K`-палитра поиска; тема (dark-first) + расход (`cost_journal`).
- **Вызываемые поверхности (оговорка 2 ADR-030)** — не линзы и не палитра, а контекстные панели: **Безопасность/RBAC** (клик на узел → «разобрать этот участок»), **Настройки среды**, **Билинг/аудит-журнал**. Привязаны к канве, вызываются, не живут вкладками-ровнями.
- **Обзор** — нулевая линза/вход: **портфель графов** (список агентов/потоков) до входа в один граф. С него «проваливаешься» в единый центр. Не равноправная вкладка.
- **Темы:** dark-first + light (обе, все состояния). **Адаптив:** ≥1280 полный (палитра+канва+инспектор); 1024 — палитра/инспектор сворачиваются в иконки/выдвижные; <768 не приоритет (десктоп-среда).

**Кликабельность каркаса:** источник в палитре → drag на канву (добавить узел); клик узла → инспектор (§3-настройки); клик связи → data-контракт; переключатель линз → тот же граф в другом режиме (§2 Строю / §3 Запускаю / §8а Эксплуатирую / §7 Конверт); рамка-конверт → §7 Паспорт-оверлей; `⌘K` → §9.3; лого → Обзор-портфель.

---

## 1. Карта экранов и переходов (user-flow прототипа)

Не дерево вкладок, а **один граф с линзами** (ADR-030). Вход — портфель, дальше всё внутри единого центра:

```
   ┌──────────────────┐
   │ ОБЗОР — портфель │◀── вход после логина (список графов/агентов)
   │ графов/агентов   │
   └────────┬─────────┘
            │ клик по графу → проваливаешься в ЕДИНЫЙ ЦЕНТР (канва)
            ▼
   ┌──────────────────────────────────────────────────────────────┐
   │                  КАНВА (граф) + КОНВЕРТ-РАМКА                  │
   │  палитра ▸ drag узлы   ·   клик узла ▸ инспектор-настройки     │
   │                                                                │
   │  ЛИНЗЫ (тот же граф):                                          │
   │   [Строю] ─── собрать поток (палитра/DnD/LLM-черновик)         │
   │   [Запускаю] ─ волны на узлах · доска · аудитор · HITL-полоса  │
   │   [Эксплуатирую] ─ зум до Организация→процесс · живые Instance  │
   │   [Конверт] ── рамка+автономия на узлах · аудит-след           │
   └───┬───────────────┬──────────────────┬───────────────────┬────┘
       │ клик узла      │ клик Instance    │ узел вне конверта  │ «развернуть»
       ▼                ▼                  ▼                    ▼
  вызвать: Данные-     линза «Запускаю»   вызвать: Безопас-    ЖЦ развёртывания
  рецепт / Навык-      (пульт этого       ность (ре-аттест.    (deploy/пауза/отзыв/
  безопасность (инсп.) прогона)           этого участка)       откат vN)
```
**Золотой путь (демо-прототип), всё в одном центре:** Обзор-портфель → открыть граф → линза **Строю** (перетащить из палитры: Навык→узел, Данные-рецепт→источник; настроить в инспекторе) → переключить на **Запускаю** («Пуск» → волны/доска/аудитор на узлах, HITL-полоса → approve) → результат; **Конверт**-рамка всё время показывает «автономия ≤ контракт · 🟢».
**Путь эксплуатации (второй сценарий):** тот же граф → линза **Эксплуатирую** (зум до процесса, живые Instance) → клик Instance → линза **Запускаю** этого прогона → узел с дрейфом 🔴 → вызвать **Безопасность** (ре-аттестация участка) → **Строю** (правка → новый draft) → «Развернуть» (откат/новый vN).

---

## 2. Обзор (Dashboard)

**Назначение:** состояние среды с первого взгляда — что бежит, что ждёт, сколько стоит.

```
CONTENT:
┌─ H1 «Обзор» ················· [＋ Новый прогон] ▓ ─┐
├───────────────────────────────────────────────────┤
│ KPI-РЯД (4 стеклянные карты):                     │
│ [Активные прогоны 3] [HITL в очереди 2] [Агентов 7]│
│ [Расход за неделю ₽ / остаток квоты ▓прогресс]     │
├───────────────────────────────────────────────────┤
│ В ПРОДЕ СЕЙЧАС (полоса-флот, ▸§8а):                │
│ 🟢 4 агента развёрнуто · 5 экземпляров живо ·      │
│ 1 в HITL · 0 ошибок ················· [Флот ▸]     │
├───────────────────────────────────────────────────┤
│ АКТИВНЫЕ ПРОГОНЫ (список-карты):                   │
│ ┌ 🐵думает · «оценка идеи X» · волна 2/3 ░░▓───┐  │
│ │  analytics·data-analyst · 0.18₽ ·· [открыть] │  │
│ └───────────────────────────────────────────────┘ │
│ ┌ 🐵ждёт вас · «дайджест почты» · HITL   [апрув]│  │
│ └───────────────────────────────────────────────┘ │
├───────────────────────────────────────────────────┤
│ ПОСЛЕДНИЕ (свёрнутый список закрытых прогонов)     │
└───────────────────────────────────────────────────┘
```
- **Компоненты:** KPI-карты (стекло), **полоса-флот «в проде сейчас»** (агрегат FR-O5: развёрнуто/живо/HITL/ошибки + семафор-точка), карта-прогона (маскот-статус + прогресс-бар волн + семья·роль + cost + CTA), список-строк.
- **Данные:** `/api/runs?active`, `/api/hitl/queue`, `/api/agents`, `GET /api/fleet/health` (полоса-флот), `cost_journal`.
- **Состояния:** пусто → «Прогонов пока нет · [Собрать первого агента]» (ведёт в Канву); полоса-флот пусто → «В проде пока никого · [Развернуть агента]» (ведёт в §8а); загрузка → скелетоны KPI+карт.
- **Кликабельность:** `[Новый прогон]` → оверлей запуска (§9.1); карта-прогона `[открыть]` → §3 Пульт этого прогона; `[апрув]` на HITL-карте → §9.2 HITL-модалка; KPI «HITL» → §3 с фильтром очереди; **полоса-флот `[Флот ▸]` → §8а Эксплуатация**.

---

## 3. Прогоны — ПУЛЬТ (★ MVP #1, главный экран)

**Назначение:** живое наблюдение исполнения — волны, доска, эстафеты, аудитор, HITL. Здесь «снимаем чёрный ящик».

```
CONTENT (2 колонки):
┌─ H1 «Прогон: оценка идеи X» ·· 🐵думает ·· [⏸ Esc-стоп] ─┐
├──────────────────────────────────────┬──────────────────┤
│ CANVAS ВОЛН (real-time, лев. 60%)     │ ДОСКА (прав.40%)  │
│                                        │  Blackboard:      │
│  Волна 1 ▓готово   Волна 2 ▓идёт       │  ·факты           │
│  ┌ analyst ┐  ┌ finance ┐             │  ·результаты      │
│  │data_query│→│dcf-valu.│  《волны》   │  ·эстафеты        │
│  │  ✓       │  │ 🌀      │             │  [лента событий]  │
│  └──────────┘  └─────────┘             │  agent·note·time  │
│       │ handoff《эстафета-орбита》      │                   │
│  ┌ critic ┐                            ├──────────────────┤
│  │risks 🌀 │                            │ ВЕРДИКТ АУДИТОРА  │
│  └─────────┘                           │ Definition of Done│
│                                        │ ✓/✗ чеклист       │
│  progress: волна 2/3 ░░▓──             │ пробелы → [ремед.]│
├────────────────────────────────────────┴──────────────────┤
│ HITL-ПОЛОСА (если есть ожидание): 🟠 «отправить письмо?»    │
│   предпросмотр ·· [Отклонить] [Разрешить ▓]                 │
└────────────────────────────────────────────────────────────┘
```
- **Canvas волн** (OpenClaw-паттерн + брендкит): узлы = агенты (карточка семья·роль·навык), статус-иконка = маскот-состояние (готово/думает-скан/стоп); стрелки волн; **«Волны»**-лоадер на активных; **«Эстафета»**-орбита на handoff; сегмент-прогресс «волна N/M».
- **Доска** (Blackboard): лента событий (факт/результат/эстафета/claim) mono, агент+время; live-append.
- **Вердикт аудитора:** Definition of Done чеклист ✓/✗, пробелы, `[Ремедиация]` если провал.
- **HITL-полоса:** появляется при action-навыке; предпросмотр (письмо/тикет), `[Разрешить]` (primary, оптимистично + undo 5с) / `[Отклонить]` (danger → модалка причины). Наследие LUDA Пульт.
- **Данные:** `/api/runs/{id}/stream` (SSE — волны/доска live), `/api/runs/{id}/audit`, `/api/hitl/{id}`.
- **Состояния:** нет активного → «Запусти прогон» (CTA); ошибка стрима → баннер + ретрай; завершён → маскот «DoD✓», canvas замирает, итог-синтез разворачивается.
- **Кликабельность:** узел агента → поповер (навыки узла + safety + per-node модель — находка DeepSeek); `[стоп]`/Esc → подтверждение остановки; `[Разрешить]`→ оптимистично скрыть+тост-undo; `[Ремедиация]` → новая под-волна на canvas; событие доски → подсветка связанного узла.

---

## 4. Навыки — КАТАЛОГ (★ MVP #2)

**Назначение:** библиотека 37 навыков с **видимой безопасностью** (permission-scoping — находка #1).

```
CONTENT:
┌─ H1 «Навыки» ·· 37 ·· [фильтр: семья ▾][режим ▾][🔍] ─┐
├───────────────────────────────────────────────────────┤
│ ГРИД КАРТОЧЕК (auto-fill, стекло 16px):               │
│ ┌ DCF Valuation ────────────────┐ ┌ Email Draft ────┐ │
│ │ id: dcf-valuation ·mono        │ │ id: email-draft │ │
│ │ Оценка стоимости по DCF…       │ │ Деловое письмо… │ │
│ │ [🔵read][🟢internal][cite✓]    │ │[🟠action→HITL]  │ │
│ │ семьи: Финансы·Аналитика       │ │[🟠egress-маска] │ │
│ └────────────────────────────────┘ └─────────────────┘ │
└───────────────────────────────────────────────────────┘
DRAWER (при клике на карточку, справа 480px):
  ┌ DCF Valuation · dcf-valuation ────────────┐
  │ БЕЗОПАСНОСТЬ: mode=read·egress=internal·cite│
  │ семафор-объяснение каждого флага           │
  │ ── Когда применять ── Метод ── DoD ──      │  ← тело SKILL.md
  │ ── Анти-паттерны ── Интеграция ──          │
  │ используется в семьях/ролях: [чипы]        │
  │ [＋ в канву]  [открыть в прогоне-тесте]    │
  └────────────────────────────────────────────┘
```
- **Карточка навыка:** заголовок Inter 600, id mono, короткое описание, **семафор-чипы безопасности** (read=индиго-нейтр, write=индиго, action=амбер-HITL, external=амбер-бордер, cite=✓), семьи-чипы.
- **Drawer** (progressive disclosure = наш `use_skill`): полное тело SKILL.md + расшифровка безопасности + где используется.
- **Данные:** `/api/skills`, `/api/skills/{id}` (тело), `/api/skills/{id}/safety`.
- **Состояния:** фильтр без результата → empty; загрузка → скелетоны-карточки (форма известна → shimmer).
- **Кликабельность:** карточка → drawer; фильтры → сужают грид; `[＋ в канву]` → §6 Канва с добавленным узлом; чип семьи → фильтр по семье.

---

## 5. Данные — РЕЦЕПТЫ + КОННЕКТОРЫ (★ MVP #3)

**Назначение:** no-code рецепт «источник → canonical» с **предпросмотром на реальной выборке** (Hermes preview-pane).

```
CONTENT (2 колонки):
┌─ H1 «Данные» ·· [Рецепты | Коннекторы] таб ·· [＋ рецепт] ─┐
├──────────────────────────────────┬────────────────────────┤
│ РЕДАКТОР РЕЦЕПТА (лев. 55%)        │ ПРЕДПРОСМОТР (прав.45%) │
│ source: [адаптер ▾ csv/http/      │ canonical-записи:       │
│         sqlite/computed]          │ ┌──────────────────┐   │
│  ·путь/URL/query···········       │ │{id, amount, cust…│   │
│ entity: [transaction ▾]           │ │ provenance·ttl   │   │
│ map:  поле→[col/cast/lookup]  [＋] │ └──────────────────┘   │
│ rules:[assert…][pii_mask…]    [＋] │ ✓ 500 записей          │
│ emit: schema·ttl                  │ ⚠ 12 невалидно по схеме│
│                                    │ [🌀 Сканирование >3с]  │
│         [Проверить ▓] [Сохранить] │                        │
└──────────────────────────────────┴────────────────────────┘
Таб «Коннекторы»: грид подключённых источников (csv/http/sqlite/computed) +
  [＋ подключить] · у http — бейдж «egress→анти-SSRF», у sqlite — «read-only».
```
- **Редактор:** форма (адаптер-селект, entity, map-строки поле→спека, rules-чипы, emit); всё no-code.
- **Предпросмотр:** результат `data_run` на выборке — canonical JSON + provenance/ttl; счётчики (записано/отброшено/невалидно по схеме); при долгом прогоне — **«Сканирование»**-лоадер.
- **Данные:** `/api/data/adapters`, `/api/data/schema/{entity}`, `POST /api/data/recipe/preview` (dry-run), `/api/data/recipes`, `/api/data/connectors`.
- **Состояния:** пустой рецепт → плейсхолдеры + подсказка; ошибка источника → баннер (различать «нет данных» vs «ошибка адаптера»); превью пусто → «0 записей — проверь map/rules».
- **Кликабельность:** адаптер-селект меняет поля source; `[Проверить]` → предпросмотр (не сохраняя); `[Сохранить]` → в список рецептов; таб Коннекторы → грид; `[подключить]` → модалка адаптера.

---

## 6. Канва — СБОРКА АГЕНТА

**Назначение:** собрать агента из семей/ролей/навыков + источников; гибрид проводной поток + LLM-узлы (миграция канвы СВОД).

```
CONTENT (3 зоны):
┌─ H1 «Сборка: Финаналитик» ·· [паспорт ▸][Запустить ▓] ──────┐
├──────────┬───────────────────────────────────┬─────────────┤
│ ПАЛИТРА   │ ХОЛСТ (поток)                     │ ИНСПЕКТОР   │
│ (лев.220) │  ┌data_query┐→┌dcf-valuation┐     │ (прав.280)  │
│ поиск🔍   │  │источник  │  │ навык·finance│    │ узел:       │
│ ▸Семьи    │  └──────────┘  └──────┬───────┘    │ роль·модель │
│  ·роли    │        ⋔ LLM-узел(ветка)           │ БЕЗОПАСНОСТЬ│
│ ▸Навыки   │              ┌───────┐             │ mode/egress │
│  семафор  │              │ вывод │             │ /cite       │
│ ▸Источники│              └───────┘             │ [слайдер    │
│ [drag→]   │  [＋ LLM-черновик потока]           │  прав]      │
└──────────┴───────────────────────────────────┴─────────────┘
```
- **Палитра:** каталог с семафор-чипами (семьи/роли/навыки/источники), drag на холст.
- **Холст:** узлы (источник/навык/LLM-решение/вывод), рёбра-поток; `[LLM-черновик потока]` — сгенерировать поток по описанию.
- **Инспектор:** свойства узла — роль, per-node модель (DeepSeek-паттерн), **безопасность + слайдер прав** (ужесточить мгновенно / ослабить → мини-аудит; макс-крит залочен — из СВОД).
- **Пересмотр развёрнутого (FR-O4):** при открытии из §8а header показывает баннер «Пересмотр развёрнутого · vN в проде»; правки создают **новый draft vN+1** (прод-версия не трогается до повторного «Развернуть»); действие `[Развернуть]` в этом режиме заменяет `[Запустить]` и ведёт в §8а с диффом версий.
- **Данные:** `/api/families`, `/api/skills`, `/api/data/adapters`, `POST /api/flow/draft` (LLM-черновик), `/api/flow/save`; для пересмотра — `GET /api/deployments/{id}` (deployed-спека → draft).
- **Состояния:** пустой холст → «Опиши агента — соберу черновик» (LLM); невалидный поток → подсветка узла + причина; режим пересмотра → баннер «vN в проде, правишь копию».
- **Кликабельность:** drag из палитры → узел; клик узла → инспектор; `[паспорт]` → §7 предпросмотр конверта; `[Запустить]` → §9.1 → §3 Пульт; **в режиме пересмотра `[Развернуть]` → §8а Развёртывания (дифф+подтверждение)**; слайдер прав → мгновенно перекрашивает семафор узла.

---

## 7. Паспорт — КОНВЕРТ (Governance)

**Назначение:** verified envelope агента — что умеет, на каких условиях, стык с контрактом LUDA (миграция паспорта из LUDA).

```
CONTENT:
┌─ H1 «Паспорт: Финаналитик» ·· 🐵DoD✓ ·· [Развернуть ▓][PDF] ─┐
├──────────────────────────────────────────────────┤
│ ПРОД-СТАТУС (полоса, FR-O1):                       │
│  🟢 в проде: v3 · участок «оценка заявок» ·        │
│  2 экземпляра живо · [История версий v1..v3 ▾]     │
│  [пауза][отзыв→v2][пересмотреть→Канва][вывести]    │
├──────────────────────────────────────────────────┤
│ КОНВЕРТ (крупная карта):                          │
│  Автономия: A2 ▓  ≤ DeploymentContract(A2) LUDA ✓ │
│  Роли·навыки: [чипы с безопасностью]               │
│  HITL-точки: письмо/тикет → оператор               │
│  Резидентность: internal · PII-маска               │
├──────────────────────────────────────────────────┤
│ FLOW-СХЕМА (мини-канва, read-only)                │
├──────────────────────────────────────────────────┤
│ АУДИТ-СЛЕД: перешаги · провенанс · cost (таблица)  │
│ Стык LUDA: 3 контракта [CapabilityRequest…]        │
└──────────────────────────────────────────────────┘
```
- **Прод-статус (стык §8а):** полоса версии/участка/живых экземпляров + ЖЦ-кнопки (FR-O4); если агент не развёрнут → `[Развернуть]` активна, полоса свёрнута в «Не в проде».
- **Данные:** `/api/agents/{id}/passport`, `/api/agents/{id}/contracts`, `/api/agents/{id}/audit`; прод-статус — `GET /api/deployments?agent={id}`, `GET /api/instances?agent={id}`.
- **Состояния:** автономия > контракта LUDA → крит-баннер «превышение конверта» (блок запуска **и деплоя**); не развёрнут → полоса «Не в проде · [Развернуть]».
- **Кликабельность:** `[Развернуть]` → модалка деплоя (§9.5) → §8а; `[пауза]/[отзыв]/[вывести]` → действия ЖЦ (§8а, подтверждения); `[пересмотреть]` → §6 Канва (режим пересмотра); `[История версий]` → дифф; `[PDF]` → выгрузка (reuse LUDA render); чип контракта → детали; узел flow → навык в §4.

---

## 8. Настройки

**Назначение:** подключения, модели, авторизация, арендаторы, биллинг.

```
Секции-табы: [Модели][Подключения][Авторизация][Арендаторы][Биллинг]
 · Модели: per-профиль (code/research) + дефолты, свап
 · Подключения: MCP-шлюз, sLAVA-прокси egress, статус
 · Авторизация: Keycloak SSO, роли (разработчик/оператор/заказчик/admin)
 · Биллинг: cost_journal — расход per-run/per-node, недельная квота
```
- **Данные:** `/api/settings/*`, `/api/me`. **Кликабельность:** табы; сохранение с тостом.

---

## 9. Модалки и оверлеи

**9.1 Запуск прогона:** цель (textarea) + превью плана волн (детерминированная декомпозиция по семьям, редактируемо) + оценка стоимости → `[Запустить ▓]` → §3.
**9.2 HITL-подтверждение:** предпросмотр действия (письмо/тикет — рендер), провенанс, `[Разрешить ▓]`/`[Отклонить→причина]`. Оптимистично + undo 5с (LUDA-паттерн).
**9.3 ⌘K-палитра:** поиск по навыкам/агентам/прогонам/командам; Enter → переход.
**9.4 Подключение адаптера:** форма креды/URL/путь + тест-прогон.
**9.5 Развернуть агента (→ §8а):** выбор версии (vN) + участок процесса + триггер (событие Data Plane / расписание / входящая задача, ADR-025) + политика автономии (≤ контракт LUDA, залочен потолок) + лимит параллельных экземпляров → превью конверта → `[Развернуть ▓]`. Проверка «автономия ≤ контракт» — блок деплоя при превышении. Для «вывести/отзыв» — отдельные подтверждения (retire = type-to-confirm + дренаж; rollback = дифф vN↔vN-1, ADR-024).

---

## 10. Общие состояния (рисовать для КАЖДОГО экрана)

| Состояние | Паттерн |
|---|---|
| **Загрузка** | скелетон (форма известна — `ab-shimmer`) или спиннер-Кольцо (неизвестна) |
| **Пусто** | иконка + одна фраза + один CTA (ведёт к действию) |
| **Ошибка** | баннер `--crit`, различать «нет данных» vs «сбой» + ретрай |
| **Долгое ожидание >3с** | «Сканирование»-лоадер + поясняющий текст (что делает) |
| **Успех действия** | тост + оптимистичное обновление (+undo где обратимо) |

---

## 11. Чек-лист приёмки прототипа (каждый экран)

- [ ] Отрисован в **dark и light** темах.
- [ ] Статусы — только семафор-токены (`--ok/--warn/--crit`), не произвольный hex.
- [ ] Один **primary** на экран; focus-ring на всех интерактивах; hover/active состояния.
- [ ] Контраст WCAG AA ≥4.5:1 (особенно текст на стекле — тестировать).
- [ ] Все 4 состояния (загрузка/пусто/ошибка/наполнено) нарисованы.
- [ ] `prefers-reduced-motion` — упрощённый мотив-кит (важно: анимаций много).
- [ ] Кликабельность: каждый `[интерактив]` ведёт в указанный экран/модалку (для прототипа).
- [ ] Маскот Эйп отражает корректное состояние прогона.
- [ ] Сверено с UI-kit (`ABOP_DESIGN_KIT.md`) и брендкитом.

---

## 8а. Эксплуатация — ФЛОТ (Fleet / Operations)

**Назначение:** управление агентами **в проде** (не разработка) — реестр развёртываний, живые экземпляры, карта процессов, здоровье флота. Здесь governance из «проверки при сборке» становится **постоянным контролем в эксплуатации** (FR-O1..O6; ADR-022 Deployment≠Run, ADR-024 версии+откат, ADR-025 триггеры, ADR-027 governance на экземпляре).
**Референс:** Пульт (§3) для инстансов, размещение LUDA (AS-IS/TO-BE) для карты процессов.
**Приоритет отрисовки:** ПОСЛЕ MVP-экранов Run (§3→§4→§5) — флот надстраивается над рабочим одиночным прогоном.

```
CONTENT:
┌─ H1 «Эксплуатация» ·· 🟢 флот здоров ·· [＋ Развернуть] ▓ ──┐
├────────────────────────────────────────────────────────────┤
│ [Развёртывания] [Живые]•5 [Карта процессов] [Здоровье]  ← табы│
└────────────────────────────────────────────────────────────┘
```
- **Общее:** 4 таба; header-статус = семафор здоровья флота (`/api/fleet/health`); primary `[＋ Развернуть]` (модалка §9.5). Бейдж «Живые•N» дублирует sidebar-счётчик.

### Таб «Развёртывания» (FR-O1 реестр + FR-O4 ЖЦ)

```
┌─ [фильтр: статус ▾][участок ▾][владелец ▾][🔍] ·· 4 ──────────┐
├──────────────────────────────────────────────────────────────┤
│ ТАБЛИЦА-РЕЕСТР (строки = прод-агенты):                        │
│ агент·версия │ участок / триггер │статус│владел.│автон.│ЖЦ    │
│ ┌──────────────────────────────────────────────────────────┐ │
│ │Финаналитик v3│«оценка заявок»/│🟢актив│@ivan │A2 ▓ │[⋯]  │ │
│ │              │событие Data    │       │      │≤LUDA │      │ │
│ ├──────────────────────────────────────────────────────────┤ │
│ │Почта-дайджест│«входящие»/     │🟡пауза│@olga │A1   │[⋯]  │ │
│ │ v1           │расписание 09:00│       │      │     │      │ │
│ ├──────────────────────────────────────────────────────────┤ │
│ │Тикет-роутер  │«support»/      │🔴отзв.│@ivan │A2   │[⋯]  │ │
│ │ v2→v1        │входящая задача │       │      │     │      │ │
│ └──────────────────────────────────────────────────────────┘ │
│ [⋯] меню ЖЦ: пауза/возобновить · отзыв→vN-1 · пересмотреть   │
│              через канву · вывести (type-to-confirm)          │
└──────────────────────────────────────────────────────────────┘
```
- **Компоненты:** таблица-реестр (строка = развёртывание); колонки имя·версия / участок·триггер / статус (семафор-чип актив/пауза/отозван) / владелец / автономия (A-уровень + «≤LUDA» бейдж конверта) / `[⋯]`-меню ЖЦ; фильтр-бар; primary `[＋ Развернуть]`.
- **Триггер-иконка:** событие Data Plane / расписание / входящая задача (ADR-025) — типизирована чипом.
- **ЖЦ-действия (FR-O4):** `пауза/возобновить` (мгновенно, тост+undo) · `отзыв версии` → откат к vN-1 (подтверждение + дифф, ADR-024) · `пересмотреть через канву` → §6 Канва (режим пересмотра, новый draft) · `вывести (retire)` → **type-to-confirm** модалка + индикатор дренажа живых экземпляров.
- **Данные:** `GET /api/deployments`; действия — `POST /api/deployments/{id}/deploy|pause|retire|rollback`.
- **Состояния:** пусто → «В проде пока никого · [Развернуть агента]» (CTA → §9.5); загрузка → скелетон-строки (форма таблицы известна → shimmer); ошибка → баннер `--crit` «реестр недоступен» + ретрай; наполнено → строки с семафор-статусами.
- **Кликабельность:** строка → §7 Паспорт этого агента (прод-статус развёрнут); `[⋯]` → меню ЖЦ; «пересмотреть» → §6 Канва; «вывести» → type-to-confirm модалка; «отзыв» → дифф-подтверждение; фильтры → сужают реестр.

### Таб «Живые» (FR-O2 экземпляры, авто-обновление)

```
┌─ live ·· ⟳ авто-обновление 2с ·· 5 экземпляров ·· [пауза-обн]─┐
├──────────────────────────────────────────────────────────────┤
│ ТАБЛИЦА ЭКЗЕМПЛЯРОВ (live-append/update):                     │
│ агент │ что делает (шаг/навык) │инициатор│длит.│cost│статус  │
│ ┌──────────────────────────────────────────────────────────┐ │
│ │Финанал.│dcf-valuation · волна2│событие  │0:12│0.18₽│🐵думает│ │
│ │Финанал.│risks · критик        │событие  │0:04│0.05₽│🌀скан  │ │
│ │Почта-д.│email-draft           │09:00 расп│0:31│0.09₽│🟠HITL  │ │
│ │Тикет-р.│classify              │задача#88│0:02│0.02₽│🔴ошибка│ │
│ │Финанал.│data_query            │событие  │0:01│0.01₽│🐵idle  │ │
│ └──────────────────────────────────────────────────────────┘ │
│ статус = маскот-состояние Эйп (idle/думает/скан/HITL/ошибка)  │
└──────────────────────────────────────────────────────────────┘
```
- **Компоненты:** таблица-инстансов (строка = живой экземпляр = помеченный авто-прогон `deployment_id+instance_id`); колонки агент / что делает (шаг·навык) / инициатор (событие·человек·расписание) / длительность (тикер) / стоимость (per-instance из cost_journal) / статус = **маскот-состояние Эйп**; индикатор авто-обновления (⟳, `[пауза-обн]`).
- **Авто-обновление:** поллинг/стрим (2с); строки live-append при старте инстанса, обновление in-place, fade-out при завершении; `prefers-reduced-motion` → без fade.
- **Данные:** `GET /api/instances` (поллинг/SSE); клик → `/api/runs/{instance_run_id}/stream` (§3).
- **Состояния:** пусто → «Экземпляров сейчас нет · развёртывания ждут триггера» (не ошибка — норма); загрузка → скелетон-строки; ошибка стрима → баннер + «обновление приостановлено» + ретрай; наполнено → живые строки с маскот-статусами.
- **Кликабельность:** строка/инстанс → **§3 Пульт этого экземпляра** (волны/доска/аудитор/HITL конкретного авто-прогона); статус 🟠HITL → §9.2 HITL-модалка; статус 🔴ошибка → §3 с раскрытой ошибкой; `[пауза-обн]` → замораживает поллинг.

### Таб «Карта процессов» (FR-O3 размещение)

```
┌─ карта ·· [процесс ▾ «Кредитный конвейер»] ·· AS-IS/TO-BE ───┐
├──────────────────────────────────────────────────────────────┤
│ ПОТОК ПРОЦЕССА (участки → агент → автономия):                 │
│  ┌заявка┐   ┌─скоринг──┐   ┌─решение─┐   ┌─уведомл.┐          │
│  │входящ│──▶│🤖Финанал.│──▶│ человек │──▶│🤖Почта- │          │
│  │      │   │ v3 · A2 ▓│   │ (не агент)│  │ дайджест│          │
│  └──────┘   └──────────┘   └─────────┘   └─────────┘          │
│               2 экз·🟢        HITL-точка     1 экз·🟡          │
│  участок без агента → [＋ посадить агента] (пунктир-слот)      │
└──────────────────────────────────────────────────────────────┘
```
- **Компоненты:** флоучарт процесса (узел = участок; на участке — карточка агента: имя·версия·автономия A-уровень·число живых экз·семафор, либо «человек»/пусто-слот); переключатель процесса; тумблер AS-IS/TO-BE (перекличка с размещением LUDA); пунктир-слот «посадить агента» на пустом участке.
- **Данные:** `GET /api/deployments` (маппинг участок→агент) + `GET /api/instances` (счётчик живых на узле); карта процесса из LUDA-размещения.
- **Состояния:** пусто → «Процессы не размещены · импортируй карту из LUDA»; загрузка → скелетон-граф; ошибка → баннер + ретрай; наполнено — граф с карточками.
- **Кликабельность:** карточка агента → §7 Паспорт; счётчик экз → §8а/Живые с фильтром по агенту; пунктир-слот `[＋ посадить агента]` → §9.5 деплой на этот участок; узел HITL-точки → пояснение.

### Таб «Здоровье» (FR-O5 агрегаты + алерты)

```
┌─ здоровье флота ·· 🟢 норма ·· окно [24ч ▾] ─────────────────┐
├──────────────────────────────────────────────────────────────┤
│ KPI-РЯД (стеклянные карты):                                  │
│ [Активные 5][Ошибки 1 🔴][В HITL 1 🟠][Стоимость флота ₽/сут]│
│ [Дрейф DoD ▓───── падение прохождения 4% ▾]                  │
├──────────────────────────────────────────────────────────────┤
│ АЛЕРТЫ (список, по серьёзности):                             │
│ 🔴 Тикет-роутер v2: 3 ошибки подряд → авто-пауза · [разбор]  │
│ 🟠 Финаналитик v3: дрейф DoD −6% за 24ч → [ре-аттестация]    │
│ 🟡 Почта-дайджест: HITL-очередь растёт (5 ждут) · [Пульт]    │
├──────────────────────────────────────────────────────────────┤
│ ТРЕНД (спарклайны): ошибки · стоимость · прохождение DoD     │
└──────────────────────────────────────────────────────────────┘
```
- **Компоненты:** KPI-карты флота (активные / ошибки / в HITL / стоимость флота из cost_journal / **дрейф DoD** — падение прохождения Definition of Done); список-алертов по серьёзности (семафор); спарклайны-тренды; селектор окна.
- **Ре-аттестация (FR-O6):** алерт дрейфа → действие `[ре-аттестация]` (стык с вкладкой Безопасность/GAPS §7 — периодический пересмотр прав прод-агента).
- **Данные:** `GET /api/fleet/health` (агрегаты + алерты + тренды).
- **Состояния:** пусто → «Флот пуст — метрик нет» (нет развёртываний); загрузка → скелетон KPI+спарклайны; ошибка → баннер + ретрай; всё зелено → «🟢 флот здоров, алертов нет».
- **Кликабельность:** KPI «Ошибки» → §8а/Живые с фильтром ошибок; KPI «В HITL» → §3 с очередью; алерт `[разбор]` → §3 Пульт инстанса с ошибкой; `[ре-аттестация]` → вкладка Безопасность (объект = развёртывание); `[Пульт]` → §3.

**Governance-инвариант (сквозной для §8а):** конверт/автономия/HITL/аудит применяются на КАЖДОМ живом экземпляре (ADR-027); превышение конверта или дрейф DoD → алерт + возможность отзыва версии (ADR-024). Автономия в реестре всегда с бейджем «≤ контракт LUDA».

---

## Приложение
- Токены/компоненты/маскот — `ABOP_DESIGN_KIT.md`
- Продукт/вкладки/FR — `ABOP_PRD.md` (v1.1)
- Флот/Операции (концепция §8а, FR-O1..O6, модель данных) — `ABOP_FLEET_OPS.md`; ре-аттестация — `ABOP_DESIGN_GAPS.md §7`; трекинг инстансов — `ABOP_RUNTIME_ARCHITECTURE.md`
- Бренд (лого/маскот/спиннеры) — `docs/brand/ABOP_Brand_Kit.dc.html`
- API-эндпоинты (`/api/...` выше) — проектируются в API-контуре ядра (следующий шаг); имена в этом доке — предварительные, синхронизировать при готовности контракта.
