fastapi>=0.110
uvicorn>=0.29
pydantic>=2.6
python-docx>=1.1     # экспорт .docx (import docx)
openpyxl>=3.1        # экспорт .xlsx
rapidocr-onnxruntime>=1.3  # OCR локально (onnxruntime), rus+eng, без внешних бинарей
Pillow>=10           # обработка изображений для OCR
# auth.py / gateway.py / db.py — только stdlib. md-экспорт — stdlib; pdf — Electron printToPDF.
