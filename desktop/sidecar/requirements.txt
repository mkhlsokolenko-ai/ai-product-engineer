fastapi>=0.110
uvicorn>=0.29
pydantic>=2.6
python-docx>=1.1     # экспорт .docx (import docx)
openpyxl>=3.1        # экспорт .xlsx
# auth.py / gateway.py / db.py — только stdlib. md-экспорт — stdlib; pdf — Electron printToPDF.
